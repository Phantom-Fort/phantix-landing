import React from "react";
import SandboxSeats from "./SandboxSeats";

/** Landing sandbox block — redirect CTA only (form lives on app.phantix.site). */
export default function SandboxSection() {
  return <SandboxSeats />;
}
