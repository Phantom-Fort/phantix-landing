# Server-only (vite proxy). Browser config: src/lib/config.ts
# Landing does not proxy sandbox-apply — it links to app.phantix.site/sandbox-apply

API_PROXY_TARGET=https://staging.phantix.site
DEV_PORT=5175
